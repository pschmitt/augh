# Fetched build artifacts (see justfile); never committed per AGENTS.md.
/dist/

# fastlane/screengrab regenerates these on every run; the actual listing content
# (fastlane/metadata/) is tracked so screenshots.yaml/play-store-images.yaml can review and
# publish it like the sibling nyetbox/jollyfin projects do.
/fastlane/report.xml
/fastlane/README.md
